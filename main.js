/**
 * Main Entry Point — Mini App con Vite
 * Punto de entrada principal de la aplicación
 */

// Las páginas HTML cargan sus propios scripts
// Este archivo se puede usar para inicialización global

console.log('🚀 Mini App iniciada con Vite');
console.log(`📦 Environment: ${import.meta.env.VITE_APP_ENV}`);
console.log(`🔒 Firebase Project: ${import.meta.env.VITE_FIREBASE_PROJECT_ID}`);
